server:
  port: 9100 # ????port?

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:sample # h2 database ????
    driver-class-name: org.h2.Driver # ??driver
    username: allen # database ???
    password: allen1234 # database ??

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

thymeleaf:
  cache: false
  prefix: classpath:/templates/    # ???templates????html??
  suffix: .html
  encoding: UTF-8
  mode: HTML5